apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: my-service   # Cloud Run service name
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: my-service:latest   # Will be replaced by Cloud Deploy
          ports:
            - containerPort: 8080
